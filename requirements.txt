# Trimmed from a full `pip freeze` (110 packages; see requirements_full.txt for
# the complete reproducible environment) down to the packages this project
# actually imports, plus the notebook runtime.

# Transitive deps (e.g. numpy, pandas, pillow, sympy, the Jupyter sub-packages) 
# are installed automatically by these and are not listed here. 

# Versions are the exact pins from the
# working environment.

# Direct imports:
#   torch        — src/model.py, src/train.py (model + training loop)
#   pypdf        — src/dataset.py (PDF parsing), scripts/md_to_pdf.py (footer stamping)
#   certifi      — src/dataset.py (HTTPS CA bundle on Windows)
#   matplotlib   — notebooks/generative_model.ipynb (loss curve + char-frequency plots)

# Notebook runtime:
#   jupyterlab   — to open/run generative_model.ipynb
#   ipykernel    — registers the project venv as a Jupyter kernel

torch==2.12.1+cu126
pypdf==6.14.2
certifi==2026.6.17
matplotlib==3.11.0
jupyterlab==4.6.0
ipykernel==7.3.0